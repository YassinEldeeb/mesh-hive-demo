extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.3"
    import: ["@key", "@external"]
  )

type Comment {
  id: ID!
  text: String!
  author: User!
}

extend type User @key(fields: "id") {
  id: ID! @external
  comments: [Comment]
}

type Query {
  comments: [Comment]
}
